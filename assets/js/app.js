var app = angular.module('WidgetSpa', [
    'ui.router'
]);

app.constant('API_URL', 'http://spa.tglrw.com:4000');

app.run(['$rootScope',
    function($rootScope) {
        
        
        
    }
]);